dentro da pasta src :

ng generate service services/buscep
ng generate service services/empresa
ng generate service services/grupo-empresas
ng generate service services/configuracaoDocumento
ng generate service services/assinatura-eletronica
ng generate service services/publicar
ng generate service services/painel
ng generate service services/contrato
ng generate service services/chamado


ng generate component app/menu
ng generate component app/rodape
ng generate component app/assinatura
ng generate component app/instrucoes 
ng generate component app/contrato 


ng generate module app/component/login --route login --module app/app.module
ng generate module app/component/grupo-empresas --route grupo-empresas --module app/app.module
ng generate module app/component/ConfiguracaoDocumento --route ConfiguracaoDocumento --module app/app.module
ng generate module app/component/assinaturas --route assinaturas --module app/app.module
ng generate module app/component/instrucoes --route instrucoes --module app/app.module
ng generate module app/component/painel --route painel --module app/app.module
ng generate module app/component/chamado --route chamado --module app/app.module


+ --------------------------------------------
npm install -g http-server

ng build 
http-server dist/front-bridge -p 8080 --spa
+ --------------------------------------------

-caso tenha atualizado o frontend
 docker stop servico
 docker rm servico


-gerar imagem se quiser garantir que ele reconstrua tudo ou o que foi implementado
 docker build -t saas-bridge .
 docker build --no-cache -t saas-bridge .

-rodar a imagem no container - descktop rodando
 docker run -d -p 8080:80 --name servico saas-bridge
 

-roda no browser
http://localhost:8080


-rodar o docker-compose
docker-compose up --build -d


-parar o docker-compose
docker-compose down

-saber se está instalado o docker-compose
docker-compose --version


+ --------------------------------------------
-builde docker postgres

# docker build -f Dockerfile.postgres -t imagem-postgres .
# docker run --name meu-postgres -p 5432:5432 -d imagem-postgres


--rodar o compose e faz tudo compila, gera image e roda
docker-compose -f docker-compose.postgres.yml up -d

--para o 
docker-compose -f docker-compose.postgres.yml down
